# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.suo
*.user

# Kiro AI workspace files (not needed in repo)
.kiro/

# Backend — handled by backend/.gitignore
backend/vendor/
backend/node_modules/
backend/public/build/
backend/public/hot
backend/public/storage
backend/.env
backend/.env.backup
backend/.env.production
backend/storage/*.key
backend/.phpunit.cache
backend/.phpunit.result.cache

# Frontend legacy (separate Vite project — no longer the main app)
frontend/node_modules/
frontend/dist/
frontend/.env
frontend/.env.local

# SQLite dev database — use MySQL in production
backend/database/database.sqlite
